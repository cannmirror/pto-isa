# Communication

`include/pto/common/pto_instr.hpp` currently does not expose explicit inter-PE communication intrinsics (for example `TPUT`/`TGET`).

If such intrinsics are added later, this page can be expanded to describe their semantics, constraints, and examples.
